# docs

各环境系统的知识根。不要在本层再堆散文件。

| 目录 | 模块 |
|---|---|
| `ocean/` | FFT 海洋 |
| `grass/` | 草地与散布 |
| `sky/` | 体积天空（planned） |
| `world/` | 大世界接线 |

每个目录必须有 `_index.md`。模块状态与可写路径在 `modules.yml`、`profiles/`。
