# global_rules

跨模块、长期有效的地板。不要把海洋打包或草 Dispatch 写进来。

| 文件 | 何时读 |
|---|---|
| `md_governance.md` | 新建、拆分、搬家控制面 Markdown 或 YAML 时 |
| `code_governance.md` | 新建或改动 C# / HLSL / compute 时 |
| `violation.md` | 超规时按表改；收工再报 |

架构分层在 `docs/world/foundation.md`。模块契约在 `docs/<module>/`。
