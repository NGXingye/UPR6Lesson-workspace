# profiles

模块边界（YAML）。可写路径以本目录为准，不要把白名单再抄进 md。

| 文件 | 模块 |
|---|---|
| `ocean.yml` | FFT 海洋 |
| `grass.yml` | 草地与散布 |
| `sky.yml` | 体积天空（planned） |
| `world.yml` | 大世界接线 |

新模块：先改 `modules.yml`，再加本目录 yml 与 `docs/<id>/_index.md`。
